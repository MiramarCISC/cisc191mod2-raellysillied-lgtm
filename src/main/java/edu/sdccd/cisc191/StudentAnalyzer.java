package edu.sdccd.cisc191;

import java.util.Comparator;
import java.util.List;

public class StudentAnalyzer {
    static List<Student> filter(List<Student>, StudentFilter) {
        // a
    }
    static void sort(List<Student>, Comparator<Student>) {
        // a
    }
    static double averageGpa(List<Student>) {
        // a
    }
}
